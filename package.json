{
  "name": "didbox-monorepo",
  "version": "0.7.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces",
    "dev": "npm run dev --workspace=@didbox/server"
  },
  "devDependencies": {
    "vitest": "^1.5.0",
    "wrangler": "^3.53.1"
  }
}
